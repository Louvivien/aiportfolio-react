# MongoDB connection string. Use the value provided by the Vercel MongoDB Atlas integration
# or copy your own Atlas/Self-hosted URI here.
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/aiportfolio?retryWrites=true&w=majority

# Optional override for the database name (the integration sets MONGODB_DATABASE automatically).
MONGODB_DB=aiportfolio

# Optional timeout overrides (milliseconds). Defaults keep serverless cold starts under 10 seconds.
# MONGODB_SERVER_SELECTION_TIMEOUT_MS=8000
# MONGODB_CONNECT_TIMEOUT_MS=8000
# MONGODB_SOCKET_TIMEOUT_MS=20000

# Local-only: Express server port.
PORT=4000
